@model IEnumerable<Projektarbete_ASP.NET.Models.Domain.Movie>
@{
    var search = ViewData["SearchWord"];
}

<div class="row">

    <div class="text-center"><h2>Results for "@search"</h2></div>
    @foreach (var movie in Model)
    {
        <partial name="_MovieCard" model="movie"/>
    }
</div>